<!DOCTYPE html>
<html lang="en">

  <head>
    <title>All Team Info</title>
    <link rel="stylesheet" href="/styles.css">
  </head>

  <body>

    <h1>Positions</h1>

      <!--Displays the entire team array of hashes-->
      <!--<%= @positionhash %>-->
      <br>

    <table>
      <tr>
        <th>Position</th>
        <th>Name</th>
        <th>Team</th>
      </tr>
      <% @positionhash.each do |nested_hash| %>
        <tr>
          <td>
            <%= "#{nested_hash[:position]}" %>
          </td>
          <td>
            <%= "#{nested_hash[:first_name]} #{nested_hash[:last_name]}" %>
          </td>
          <td>
            <%= "#{nested_hash[:team]}" %>
          </td>
        </tr>
        <% end %>
    </table>

    <br><br>


    <a href="..">Go back home</a>


  </body>

</html>

